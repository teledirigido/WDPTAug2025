function sum(a,b) {
  return a + b;
}

const result = sum(10,1000);

function greeting(firstName, LastName) {
  // "Your first name if <firstName> and your last name <LastName>"
}

const myGreeting = greeting('John', 'Doe')